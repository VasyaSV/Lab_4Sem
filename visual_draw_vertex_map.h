#ifndef VISUAL_DRAW_VERTEX_MAP_H
#define VISUAL_DRAW_VERTEX_MAP_H

#include <QObject>

using namespace std;

class visual_draw_vertex_map : public QObject
{
    Q_OBJECT
public:
    explicit visual_draw_vertex_map(QObject *parent = 0);
    
signals:
    
public slots:
    
};

#endif // VISUAL_DRAW_VERTEX_MAP_H
